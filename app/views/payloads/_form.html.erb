<%= form_with(model: payload) do |form| %>
  <% if payload.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(payload.errors.count, "error") %> prohibited this payload from being saved:</h2>

      <ul>
        <% payload.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
